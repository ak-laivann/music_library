import { RootRouter } from "../RootRouter";

// this container will serve as an area where anything that needs to be out of the UI like any standalone feature can be added
export const RootContainer = () => {
  return <RootRouter />;
};
